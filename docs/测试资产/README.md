# 测试资产

本目录保存机器可执行或可读取的评测资源，不按普通文档的新旧状态自动删除。

- `执行脚本/当前可用/`：仍有明确目标和当前调用路径的脚本；
- `执行脚本/待重新评估/`：目标接口或测试套件较旧，运行前必须核验；
- `测试样例/`：自动化评测 fixture；
- `知识库样例/`：知识库和 RAG 输入样例；
- `论文质量样例/`：论文质量评测说明与样例；
- `运行结果/`：本地生成结果，默认由 Git 和 Docker 构建上下文忽略。

移动脚本或 fixture 时必须同步修改脚本默认路径、README、忽略规则和所有自动化调用方。
