# 待重新评估

这里的文档可能仍有价值，但已经不能直接指导当前 V2 实现。常见原因包括：

- 描述 legacy `/chat`、Harness 或旧工具注册；
- 接口、部署脚本或运行环境已经变化；
- 能力尚未进入当前 V2 tool catalog；
- 仍需根据真实需求重新冻结权限、预算和验收范围。

使用前必须对照当前代码重新审查；确认继续实施后，应把新合同写入 `../当前有效/`，不要直接在旧方案上续写。
