feat(paper): start polishing from workspace and project conversations

Reuse existing paper orchestration for owned paper input and frozen project source. Add replay-safe start tool and expose start/status/result/cancel in both chat and Project; return paper task/result links, never claim queue=completion or overwrite Project files. Missing input prompts actionable upload instructions. Owned paths: product paper tool adapters/tests and directly required paper task idempotency persistence (V103 if required); shared tool policy/registration/gateway/Engine prompt bounded polling plus tests. Acceptance US2 FR003/004/008 T201-203.

Implementation authorized directly in local codex/issue-185-react-optimization by user on 2026-09-09. Shared specification: specs/001-user-capability-expansion/spec.md; plan.md and tasks.md are frozen implementation contract. No agent-v2 core or broker changes; Redis optimization is follow-up. Preserve existing data, auth, immutable ProjectVersion and publication proofs. Add success/failure/isolation tests, report exact verification and live checks not run. Do not merge PRs.

Integration refinement: ToolExecutionContext and AgentRuntimeRequest carry a server-composed invocationScope. AgentService uses sessionId + HTTP clientRequestId digest (persisted turn ID if absent); PlanAgentService uses persisted planId/stepId across retries. V103 provides durable owner/scope/input/task binding across new turns or processes. Request copies preserve scope; client-influenced traceId is observability only, never the idempotency key. The HTTP idempotency key is not identity/permission and cannot be supplied in model tool arguments. These directly affected product integration files/tests and AgentStrategySelector's native capability reachability are owned paths. Start supports original owned .tex/.bib sources, not arbitrary scripts, URLs or project publication. V103 dispatch READY survives failures before worker claim; a process crash after durable STARTED claim does not automatically reexecute the task. Paper polling is per target, preserving other task queries in the same batch; legacy literature behavior remains unchanged.
