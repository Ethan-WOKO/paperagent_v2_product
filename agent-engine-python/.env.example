# Copy to .env once; .env is ignored by Git.
# Must match Java's YANBAN_AGENT_PYTHON_SERVICE_TOKEN (at least 32 characters).
PAPERAGENT_PYTHON_TOKEN=
# Must match Java's existing ReAct engine-service-token.
PAPERAGENT_PYTHON_JAVA_SERVICE_TOKEN=
