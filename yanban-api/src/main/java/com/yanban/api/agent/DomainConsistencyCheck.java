package com.yanban.api.agent;

/** Server-selected deterministic check. Selection requests a check but never proves its result. */
public enum DomainConsistencyCheck {
    EVIDENCE_FILE_HASH_EQUALITY
}
