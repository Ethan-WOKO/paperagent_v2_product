package io.paperagent.v2.contracts;

public enum Capability {
    READ_PROJECT,
    WRITE_WORKSPACE,
    EXECUTE_COMMAND,
    INSTALL_DEPENDENCY,
    ACCESS_NETWORK,
    USE_SECRET_REFERENCE,
    INVOKE_EXTERNAL_TOOL
}
