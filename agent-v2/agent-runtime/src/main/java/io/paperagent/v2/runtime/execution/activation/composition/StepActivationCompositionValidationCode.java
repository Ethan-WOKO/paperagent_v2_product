package io.paperagent.v2.runtime.execution.activation.composition;

public enum StepActivationCompositionValidationCode {
    REQUIRED_VALUE_MISSING,
    INVALID_IDENTIFIER,
    INVALID_OUTCOME_STATE,
    INCONSISTENT_REQUEST_AUTHORITY
}
