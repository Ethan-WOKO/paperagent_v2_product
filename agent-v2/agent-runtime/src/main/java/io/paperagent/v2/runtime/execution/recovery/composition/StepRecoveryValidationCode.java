package io.paperagent.v2.runtime.execution.recovery.composition;

public enum StepRecoveryValidationCode {
    REQUIRED_VALUE_MISSING,
    INVALID_IDENTIFIER,
    INVALID_OUTCOME_STATE,
    INCONSISTENT_RECOVERY_AUTHORITY
}
